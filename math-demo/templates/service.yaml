apiVersion: v1
kind: Service
metadata:
  name: {{ include "math-demo.fullname" . }}
  labels:
    {{- include "math-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "math-demo.selectorLabels" . | nindent 4 }}
